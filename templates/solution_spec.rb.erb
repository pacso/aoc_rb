# frozen_string_literal: true
require 'spec_helper'

RSpec.describe Year<%= @year %>::Day<%= @day %> do
  let(:puzzle) { Year<%= @year %>::Day<%= @day %>.new }

  describe "part 1" do
    let(:example_input) {
      <<~EOF
        change
        me
      EOF
    }

    it "returns X for the example input" do
      expect(puzzle.part_1(example_input)).to eq("X")
    end
  end

  describe "part 2" do
    let(:example_input) {
      <<~EOF
        change
        me
      EOF
    }

    it "returns X for the example input" do
      expect(puzzle.part_2(example_input)).to eq("X")
    end
  end
end
